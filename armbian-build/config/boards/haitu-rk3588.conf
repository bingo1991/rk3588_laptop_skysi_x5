# Rockchip RK3588 SoC octa core 8GB SoC eMMC 2x USB2 1x USB3 1x USB-C 2x 1GBe 3x HDMI 2x SATA
BOARD_NAME="HT-RK3588"
BOARD_VENDOR="haitu"
BOARDFAMILY="rockchip-rk3588"
BOARD_MAINTAINER="bingo1991"
BOOT_SOC="rk3588"
BOOTCONFIG="haitu-rk3588_defconfig"
KERNEL_TARGET="edge,vendor,current"
KERNEL_TEST_TARGET="edge,vendor,current"
FULL_DESKTOP="yes"
BOOT_LOGO="desktop"
BOOT_FDT_FILE="rockchip/rk3588-haitu.dtb"
BOOT_SCENARIO="spl-blobs"
IMAGE_PARTITION_TABLE="gpt"

declare -g UEFI_EDK2_BOARD_ID="haitu-rk3588" # This _only_ used for uefi-edk2-rk3588 extension

function post_family_tweaks__haiturk3588_naming_audios() {
	display_alert "$BOARD" "Renaming haitu HT-RK3588 audios" "info"

	mkdir -p $SDCARD/etc/udev/rules.d/
	echo 'SUBSYSTEM=="sound", ENV{ID_PATH}=="platform-hdmi0-sound", ENV{SOUND_DESCRIPTION}="HDMI0 Audio"' >> $SDCARD/etc/udev/rules.d/90-naming-audios.rules
	echo 'SUBSYSTEM=="sound", ENV{ID_PATH}=="platform-hdmi1-sound", ENV{SOUND_DESCRIPTION}="HDMI1 Audio"' >> $SDCARD/etc/udev/rules.d/90-naming-audios.rules
	echo 'SUBSYSTEM=="sound", ENV{ID_PATH}=="platform-hdmiin-sound", ENV{SOUND_DESCRIPTION}="HDMI-In Audio"' >> $SDCARD/etc/udev/rules.d/90-naming-audios.rules
	echo 'SUBSYSTEM=="sound", ENV{ID_PATH}=="platform-dp0-sound", ENV{SOUND_DESCRIPTION}="DP0 Audio"' >> $SDCARD/etc/udev/rules.d/90-naming-audios.rules

	return 0
}
